{
  "$id": "https://zeitlines.dev/schema/timeline.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/TimelineFile",
  "definitions": {
    "TimelineFile": {
      "type": "object",
      "properties": {
        "$schema": {
          "type": "string",
          "description": "Points editors at schema/timeline.schema.json for completion + validation."
        },
        "name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "groupBy": {
          "type": "string"
        },
        "groupOrder": {
          "type": "string",
          "enum": [
            "alpha",
            "declared"
          ],
          "description": "In what order the groups are laid out: alphabetically by id (the default and the behaviour every existing timeline has), or in the order `groups` declares.\n\n`alpha` is the default rather than the obvious `declared` because it is what shipped: the committed examples number their group ids (`1-strategy`, `2-design`) precisely to steer the alphabetical sort, and flipping the default would silently reorder the lanes of every timeline in existence — including ones whose declared order was never meant to be read.\n\n`declared` is for the case the workaround cannot reach: group ids that carry meaning of their own and cannot be renumbered, such as the folder names a directory source derives its groups from. Groups that only appear on items, with no declaration, follow the declared ones alphabetically either way."
        },
        "graph": {
          "$ref": "#/definitions/GraphConfig",
          "description": "How the relation graph reads this timeline. See `GraphConfig`."
        },
        "savedViews": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/SavedView"
          },
          "description": "Named ways of looking at this timeline. On a `db` source these are rows of the `saved_views` table folded in for the caller; in a file they are the store itself, which is what keeps a local timeline self-contained — the same split `pluginData` makes (docs/plugin-storage.md).\n\nWhat reaches a client is already filtered by who asked: a private view of somebody else is never in this array."
        },
        "plugins": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PluginRef"
          }
        },
        "pluginData": {
          "$ref": "#/definitions/PluginData"
        },
        "phases": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TimelinePhase"
          }
        },
        "customFields": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CustomFieldDef"
          }
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TimelineFileItem"
          }
        },
        "groups": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string"
              },
              "content": {
                "type": "string"
              },
              "nestedGroups": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "showNested": {
                "type": "boolean"
              },
              "color": {
                "type": "string",
                "description": "The group's own colour, any CSS colour. Unset falls back to the positional lane palette, so no existing timeline changes appearance.\n\nIt exists because the lane palette answers „which track is this\" and some timelines need „what kind of thing is this\": a hint is green and an antagonist's move is red because of what they mean, not because of which column they landed in. That mapping is the author's to make, which is why it is a value in the data rather than a rule in the code — the same reasoning behind `phases[].color` and `customFields[].options[].color`.\n\n**Honoured by the relation graph only, for now.** The chart's lane colouring runs through `.lane-N` selectors on the item elements, and widening that to arbitrary per-group colours is its own change to the drawing path; claiming this field is global while half the app ignored it would be worse than the documented limit."
              }
            },
            "required": [
              "id",
              "content"
            ],
            "additionalProperties": false
          }
        }
      },
      "required": [
        "items"
      ],
      "additionalProperties": false
    },
    "GraphConfig": {
      "type": "object",
      "properties": {
        "bandRootGroup": {
          "type": "string",
          "description": "The group whose items are **band roots**: each one claims everything reachable from it, its title becomes the band's heading, and it is taken out of the columns.\n\nWithout it a band is an anonymous connected component, which is honest but says nothing. With it, a strand is labelled by the thing it hangs off — the plan a set of tasks serves, the epic a set of stories belongs to. Members nothing claims fall back to anonymous components behind the titled ones."
        },
        "referenceGroup": {
          "type": "string",
          "description": "The group whose items are shown **on** a node rather than beside it: every item of that group which references this node is listed under its title.\n\nIt exists for the references that are context and not structure. A revelation surfaces in four scenes; drawn as four edges that is noise, and it also disappears the moment the extent hides scenes. As a line on the node it survives both."
        }
      },
      "additionalProperties": false,
      "description": "What the relation graph needs told about a timeline, beyond its items and relations.\n\nBoth fields name a **group**, because that is the vocabulary a timeline already has for „what kind of thing is this\" — no new dimension, and a folder-derived group works as well as a declared one."
    },
    "SavedView": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique within its timeline; travels in the hash as `sv=<id>`."
        },
        "name": {
          "type": "string"
        },
        "mode": {
          "type": "string",
          "description": "The presentation to open in — a `ViewMode` string (`timeline`, `list`, `plugin:<id>:<view>`). Absent means „leave the presentation alone\", which is what makes a view that is only about the narrowing possible."
        },
        "groupBy": {
          "type": "string",
          "description": "The grouping dimension, as `state.groupBy` spells it (`group`, `cf:tier`, …)."
        },
        "filters": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "description": "Selected values per filter dimension; the shape `FilterSelection` describes."
        },
        "edges": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "enum": [
              "off",
              "in",
              "out"
            ]
          },
          "description": "Which recorded link fields become edges, and which way they point (see src/linkEdges.ts). Only fields deviating from the default are stored, so an absent value means „every field incoming\" — which is what every view saved before this existed was showing.\n\nRound-trips through a **local** source, whose saved views are stored as this JSON. The database path enumerates its columns and has no `edges` one, so a DB-backed view drops it — deliberately, because only a directory scan records the link origins this acts on, and the control never appears without them. A DB source that ever records them needs the migration before it needs this."
        },
        "owner": {
          "type": "string",
          "description": "The address of whoever created it. Empty on a source with no identity."
        },
        "visibility": {
          "$ref": "#/definitions/SavedViewVisibility",
          "description": "Defaults to `private` when absent, which is what an older file spells."
        },
        "version": {
          "type": "number",
          "description": "Optimistic-lock counter, server-managed like an item's."
        },
        "createdAt": {
          "type": "string"
        },
        "createdBy": {
          "type": "string"
        },
        "updatedAt": {
          "type": "string"
        },
        "updatedBy": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "name"
      ],
      "additionalProperties": false,
      "description": "A named combination of presentation, grouping dimension and filter selection, stored with the timeline rather than in one person's browser.\n\nThe display state itself is per person and per timeline in `localStorage` (docs/editing.md → „Where the display state lives\"). That answers „how am I looking at this right now\" and cannot answer „the way we look at this every Monday\", because it has no name, no second reader and no address.\n\nWhat it deliberately does NOT carry is the visible time window. An absolute window ages into something nobody wants to see within a quarter, and „the next three months\" is a relative window — a different feature that this one must not smuggle in under the same name."
    },
    "SavedViewVisibility": {
      "type": "string",
      "enum": [
        "private",
        "instance"
      ],
      "description": "Who a saved view is for.\n\n`private` is the default because a half-built narrowing is the normal state of one: somebody sets a filter, keeps it, and refines it over a week. Publishing is a separate act, and it needs `write` — a `viewer` may keep their own without being able to put one in front of everybody (see docs/users.md)."
    },
    "PluginRef": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "config": {
          "type": "object",
          "additionalProperties": {}
        },
        "public": {
          "type": "boolean",
          "description": "Consent to serve this plugin's declared `publicRead` collections without authentication. Absent = no.\n\nSeparate from being enabled, and deliberately so: plenty of timelines carry a pricing model that is not meant to be public, and „the plugin is on\" must not be read as „the world may read it\". On a `db` source this is the `timeline_plugins.public` column; here it is the same fact on the file, so the decision travels with the document a user owns."
        }
      },
      "required": [
        "id"
      ],
      "additionalProperties": false
    },
    "PluginData": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/PluginCollectionData"
      },
      "description": "Every enabled plugin's rows, keyed by plugin id.\n\nIt sits on the timeline file rather than behind a second request because a local source has no server to ask: a static deploy materializes the file and that copy has to be complete, or the plugin renders nothing. Making the DB path match means one payload shape for both, and it keeps a local timeline self-contained — copying the file copies the plugin's data with it.\n\nOnly plugins actually enabled on the timeline are included, which is the same gate that decides whether their code loads at all."
    },
    "PluginCollectionData": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "$ref": "#/definitions/PluginDataRow"
        }
      },
      "description": "A plugin's collections, keyed by the collection id its manifest declares."
    },
    "PluginDataRow": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "data": {
          "type": "object",
          "additionalProperties": {}
        },
        "version": {
          "type": "number"
        },
        "updatedAt": {
          "type": "string"
        },
        "updatedBy": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "data"
      ],
      "additionalProperties": false,
      "description": "One row of a collection a plugin owns, in the shape it travels in everywhere: the wire, the host API, and the section a local file carries.\n\n`data` is the plugin's own object, validated against the collection's declared JSON Schema on the write path. Everything beside it is host-managed and a plugin never sets it.\n\n`version` is the optimistic-lock counter sent back as `If-Match`. What it counts differs by backing store, and the difference is real rather than cosmetic: a DB row has its own counter, so two people can edit two rows of one collection at once; a local file has one version for the whole document, so the same header there means „the file has not changed since you read it\". Items already work exactly this way — see „Locking\" in docs/plugin-storage.md.\n\nThere is no `sort` field. Order is the array's order in `PluginCollectionData`, which is the only representation a JSON file has; the DB's `sort` column exists to reproduce it and stays behind the repo."
    },
    "TimelinePhase": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "start": {
          "type": "string"
        },
        "end": {
          "type": "string"
        },
        "duration": {
          "type": [
            "string",
            "number"
          ]
        },
        "color": {
          "type": "string"
        },
        "icon": {
          "type": "string"
        }
      },
      "required": [
        "label",
        "start"
      ],
      "additionalProperties": false
    },
    "CustomFieldDef": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "type": {
          "$ref": "#/definitions/CustomFieldType"
        },
        "options": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CustomFieldOption"
          }
        },
        "group": {
          "type": "string"
        },
        "width": {
          "type": "string",
          "enum": [
            "half",
            "full"
          ]
        },
        "contextMenu": {
          "type": "boolean"
        },
        "derived": {
          "type": "boolean"
        }
      },
      "required": [
        "key",
        "label",
        "type"
      ],
      "additionalProperties": false
    },
    "CustomFieldType": {
      "type": "string",
      "enum": [
        "text",
        "select",
        "multi-select"
      ]
    },
    "CustomFieldOption": {
      "type": "object",
      "properties": {
        "value": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "color": {
          "type": "string"
        }
      },
      "required": [
        "value"
      ],
      "additionalProperties": false
    },
    "TimelineFileItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "start": {
          "type": "string"
        },
        "end": {
          "type": "string"
        },
        "duration": {
          "type": [
            "string",
            "number"
          ]
        },
        "content": {
          "type": "string"
        },
        "group": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "point",
            "range",
            "background",
            "box"
          ]
        },
        "className": {
          "type": "string"
        },
        "icon": {
          "type": "string"
        },
        "status": {
          "$ref": "#/definitions/StatusKey"
        },
        "body": {
          "type": "string"
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {}
        },
        "version": {
          "type": "number"
        },
        "createdAt": {
          "type": "string"
        },
        "createdBy": {
          "type": "string"
        },
        "updatedAt": {
          "type": "string"
        },
        "updatedBy": {
          "type": "string"
        }
      },
      "required": [
        "content"
      ],
      "additionalProperties": false
    },
    "StatusKey": {
      "type": "string",
      "enum": [
        "Open",
        "Doing",
        "Done"
      ]
    }
  }
}
