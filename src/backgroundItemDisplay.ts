// Display seam shared by the background builder and the DOM decorators. Keeping
// the class token here prevents presence from importing the whole item-building
// graph merely to recognise the generated title range.
export const BACKGROUND_LABEL_CLASS = 'background-item-label';
